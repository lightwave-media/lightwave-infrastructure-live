# Account-level configuration for global (shared) resources
# These resources are used across all environments

locals {
  account_name   = "global"
  aws_account_id = "738605694078" # LightWave Media production account
}
